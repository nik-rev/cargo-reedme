@@ -4,13 +4,13 @@
 
 ## The decoder
 
-The most important types for decoding purposes are [`Decoder`] and
-[`Reader`]. They both wrap a [`std::io::Read`].
-`Decoder` serves as a builder for `Reader`. Calling [`Decoder::read_info`] reads from the `Read` until the
+The most important types for decoding purposes are [`Decoder`](https://docs.rs/png/0.18.1/png/decoder/struct.Decoder.html) and
+[`Reader`](https://docs.rs/png/0.18.1/png/decoder/struct.Reader.html). They both wrap a [`std::io::Read`](https://doc.rust-lang.org/stable/std/io/trait.Read.html).
+`Decoder` serves as a builder for `Reader`. Calling [`Decoder::read_info`](https://docs.rs/png/0.18.1/png/decoder/struct.Decoder.html#method.read_info) reads from the `Read` until the
 image data is reached.
 
 ### Using the decoder
-```
+```rust
 use std::fs::File;
 use std::io::BufReader;
 // The decoder is a build for reader and can be used to set various decoding options
@@ -30,7 +30,7 @@
 ## Encoder
 ### Using the encoder
 
-```no_run
+```rust
 // For reading and opening files
 use std::path::Path;
 use std::fs::File;
