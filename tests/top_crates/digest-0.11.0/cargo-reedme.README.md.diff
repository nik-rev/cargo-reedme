@@ -25,7 +25,7 @@
 ```
 
 `sha2` and other crates re-export `digest` crate and `Digest` trait for
-convenience, so you don't have to add `digest` crate as an explicit dependency.
+convenience, so you don’t have to add `digest` crate as an explicit dependency.
 
 Now you can write the following code:
 
@@ -137,21 +137,21 @@
 
 Traits in this crate are organized into the following levels:
 
-- **High-level convenience traits**: [`Digest`], [`DynDigest`], [`Mac`].
+- **High-level convenience traits**: [`Digest`](https://docs.rs/digest/0.11.0/digest/digest/trait.Digest.html), [`DynDigest`](https://docs.rs/digest/0.11.0/digest/digest/trait.DynDigest.html), [`Mac`](https://docs.rs/digest/0.11.0/digest/mac/trait.Mac.html).
   Wrappers around lower-level traits for most common use-cases. Users should
   usually prefer using these traits.
-- **Mid-level traits**: [`Update`], [`FixedOutput`], [`FixedOutputReset`], [`ExtendableOutput`],
-  [`ExtendableOutputReset`], [`XofReader`], [`Reset`], [`KeyInit`], and [`InnerInit`].
+- **Mid-level traits**: [`Update`](https://docs.rs/digest/0.11.0/digest/trait.Update.html), [`FixedOutput`](https://docs.rs/digest/0.11.0/digest/trait.FixedOutput.html), [`FixedOutputReset`](https://docs.rs/digest/0.11.0/digest/trait.FixedOutputReset.html), [`ExtendableOutput`](https://docs.rs/digest/0.11.0/digest/trait.ExtendableOutput.html),
+  [`ExtendableOutputReset`](https://docs.rs/digest/0.11.0/digest/trait.ExtendableOutputReset.html), [`XofReader`](https://docs.rs/digest/0.11.0/digest/trait.XofReader.html), [`Reset`](https://docs.rs/crypto_common/latest/crypto_common/trait.Reset.html), [`KeyInit`](https://docs.rs/crypto_common/latest/crypto_common/trait.KeyInit.html), and [`InnerInit`](https://docs.rs/crypto_common/latest/crypto_common/trait.InnerInit.html).
   These traits atomically describe available functionality of an algorithm.
-- **Marker traits**: [`HashMarker`], [`MacMarker`]. Used to distinguish
+- **Marker traits**: [`HashMarker`](https://docs.rs/digest/0.11.0/digest/digest/trait.HashMarker.html), [`MacMarker`](https://docs.rs/digest/0.11.0/digest/mac/trait.MacMarker.html). Used to distinguish
   different algorithm classes.
-- **Low-level traits** defined in the [`block_api`] module. These traits
+- **Low-level traits** defined in the [`block_api`](https://docs.rs/digest/0.11.0/digest/block_api/) module. These traits
   operate at a block-level and do not contain any built-in buffering.
   They are intended to be implemented by low-level algorithm providers only.
   Usually they should not be used in application-level code.
 
 Additionally hash functions implement traits from the standard library:
-[`Default`] and [`Clone`].
+[`Default`](https://doc.rust-lang.org/stable/core/default/trait.Default.html) and [`Clone`](https://doc.rust-lang.org/stable/core/clone/trait.Clone.html).
 
 This crate does not provide any implementations of the `io::Read/Write` traits,
 see the [`digest-io`] crate for `std::io`-compatibility wrappers.
