@@ -30,7 +30,7 @@
    manipulations like insert a key in a particular spot.
  - **As a strongly typed Rust data structure.** When you expect all or most
    of your data to conform to a particular structure and want to get real
-   work done without JSON's loosey-goosey nature tripping you up.
+   work done without JSON’s loosey-goosey nature tripping you up.
 
 Serde JSON provides efficient, flexible, safe ways of converting data
 between each of these representations.
@@ -40,10 +40,7 @@
 Any valid JSON data can be manipulated in the following recursive enum
 representation. This data structure is [`serde_json::Value`][value].
 
-```
-# use serde_json::{Number, Map};
-#
-# #[allow(dead_code)]
+```rust
 enum Value {
     Null,
     Bool(bool),
@@ -55,11 +52,11 @@
 ```
 
 A string of JSON data can be parsed into a `serde_json::Value` by the
-[`serde_json::from_str`][from_str] function. There is also [`from_slice`]
-for parsing from a byte slice `&[u8]` and [`from_reader`] for parsing from
+[`serde_json::from_str`][from_str] function. There is also [`from_slice`](https://docs.rs/serde_json/1.0.149/serde_json/de/fn.from_slice.html)
+for parsing from a byte slice `&[u8]` and [`from_reader`](https://docs.rs/serde_json/1.0.149/serde_json/de/fn.from_reader.html) for parsing from
 any `io::Read` like a File or a TCP stream.
 
-```
+```rust
 use serde_json::{Result, Value};
 
 fn untyped_example() -> Result<()> {
@@ -82,10 +79,6 @@
 
     Ok(())
 }
-#
-# fn main() {
-#     untyped_example().unwrap();
-# }
 ```
 
 The result of square bracket indexing like `v["name"]` is a borrow of the
@@ -105,7 +98,7 @@
 
 [`as_str()`]: crate::Value::as_str
 
-The `Value` representation is sufficient for very basic tasks but can be
+The `Value` representatihttps://docs.rs/serde_json/1.0.149/serde_json/value/enum.Value.html#method.as_str very basic tasks but can be
 tedious to work with for anything more significant. Error handling is
 verbose to implement correctly, for example imagine trying to detect the
 presence of unrecognized fields in the input data. The compiler is powerless
@@ -117,7 +110,7 @@
 Serde provides a powerful way of mapping JSON data into Rust data structures
 largely automatically.
 
-```
+```rust
 use serde::{Deserialize, Serialize};
 use serde_json::Result;
 
@@ -150,10 +143,6 @@
 
     Ok(())
 }
-#
-# fn main() {
-#     typed_example().unwrap();
-# }
 ```
 
 This is the same `serde_json::from_str` function as before, but this time we
@@ -162,7 +151,7 @@
 error messages if the layout does not conform to what a `Person` is expected
 to look like.
 
-Any type that implements Serde's `Deserialize` trait can be deserialized
+Any type that implements Serde’s `Deserialize` trait can be deserialized
 this way. This includes built-in Rust standard library types like `Vec<T>`
 and `HashMap<K, V>`, as well as any structs or enums annotated with
 `#[derive(Deserialize)]`.
@@ -179,7 +168,7 @@
 Serde JSON provides a [`json!` macro][macro] to build `serde_json::Value`
 objects with very natural JSON syntax.
 
-```
+```rust
 use serde_json::json;
 
 fn main() {
@@ -208,11 +197,7 @@
 will check at compile time that the value you are interpolating is able to
 be represented as JSON.
 
-```
-# use serde_json::json;
-#
-# fn random_phone() -> u16 { 0 }
-#
+```rust
 let full_name = "John Doe";
 let age_last_year = 42;
 
@@ -239,7 +224,7 @@
 [`serde_json::to_writer`][to_writer] which serializes to any `io::Write`
 such as a File or a TCP stream.
 
-```
+```rust
 use serde::{Deserialize, Serialize};
 use serde_json::Result;
 
@@ -264,13 +249,9 @@
 
     Ok(())
 }
-#
-# fn main() {
-#     print_an_address().unwrap();
-# }
 ```
 
-Any type that implements Serde's `Serialize` trait can be serialized this
+Any type that implements Serde’s `Serialize` trait can be serialized this
 way. This includes built-in Rust standard library types like `Vec<T>` and
 `HashMap<K, V>`, as well as any structs or enums annotated with
 `#[derive(Serialize)]`.
@@ -278,8 +259,8 @@
 # No-std support
 
 As long as there is a memory allocator, it is possible to use serde_json
-without the rest of the Rust standard library. Disable the default "std"
-feature and enable the "alloc" feature:
+without the rest of the Rust standard library. Disable the default “std”
+feature and enable the “alloc” feature:
 
 ```toml
 [dependencies]
@@ -290,11 +271,9 @@
 [`serde-json-core`] crate.
 
 [value]: crate::value::Value
-[from_str]: crate::de::from_str
+[from_str]: https://docs.rs/serde_json/1.0.149/serde_json/de/fn.from_str.html
 [from_slice]: crate::de::from_slice
 [from_reader]: crate::de::from_reader
 [to_string]: crate::ser::to_string
-[to_vec]: crate::ser::to_vec
-[to_writer]: crate::ser::to_writer
-[macro]: crate::json
+[to_vec]: crate::ser::to_vechttps://docs.rs/serde_json/1.0.149/serde_json/ser/fn.to_string.htmlser::to_wrihttps://docs.rs/serde_json/1.0.149/serde_json/ser/fn.to_vec.html://docs.rs/serhttps://docs.rs/serde_json/1.0.149/serde_json/ser/fn.to_writer.html_json/macro.json.html
 [`serde-json-core`]: https://github.com/rust-embedded-community/serde-json-core
\ No newline at end of file
